pipeline {
    agent any
    stages { 
        stage('Clone') {
            sh 'Clone pipeline'
        }
        stage('Build') {
            sh 'Build pipeline'
        }
        stage('Test') {
           sh 'Test pipeline'
        }
        stage('Deploy') {
            sh 'Deploy pipeline'
        }
    }
}
