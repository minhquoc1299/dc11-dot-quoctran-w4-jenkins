pipeline {
    agent any
    stages { 
        stage('Clone') {
            steps{
                echo 'Clone pipeline 2 - production'
            }
        }
        stage('Build') {
           steps{
                echo 'Clone pipeline 2 - production'
            }
        }
        stage('Test') {
           steps{
                echo 'Clone pipeline 2 - production'
            }
        }
        stage('Deploy') {
            steps{
                echo 'Clone pipeline 2 - production'
            }
        }
    }
}